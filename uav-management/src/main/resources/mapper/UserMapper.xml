<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uav.management.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.uav.management.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="salt" property="salt" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="last_login_time" property="lastLoginTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, salt, role, status, create_time, last_login_time
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE id = #{id}
    </select>

    <update id="updateLastLoginTime" parameterType="com.uav.management.entity.User">
        UPDATE user
        SET last_login_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
